# Finding 047
- Severity: Low
- Pattern type: AI slop; Billing UI formatting duplication
- File + line(s): `apps/jskit-value-app/src/views/console/useConsoleBillingPlansView.js:363-387`, `apps/jskit-value-app/src/views/console/useConsoleBillingProductsView.js:30-55`
- Why it can drift/fail: Both console billing views duplicate `formatMoneyMinor` and `formatInterval` helpers with slightly different defaults. This is shared UI formatting logic that should be centralized.
- Concrete impact: Inconsistent pricing/interval formatting between billing views and higher maintenance overhead for formatting changes.
- Exact remediation: Extract shared billing UI formatters into a common helper module and reuse across views.

---
WONT FIX
Reason:
- This finding is client-side (`apps/jskit-value-app/src/views/...`), and you asked me to avoid client-side work while templates are being rewritten.

Revisit signal:
- Once client-side work is unblocked, I can extract shared billing UI formatters and reuse across views.

---
DONE SUMMARY
- Extracted `formatMoneyMinor` and `formatInterval` into `apps/jskit-value-app/src/views/console/billingFormatters.js`.
- Updated billing plans/products views to consume shared formatters.
- Kept products-specific empty-interval behavior via explicit `emptyLabel: "one-time"` option.

TESTS RUN
- `npm -ws --if-present test` (fails in environment: missing workspace executables/dependencies and module resolution errors)
- `npm run test:framework` (fails in environment: migration tracker mismatch + missing `@jskit-ai/cli-entrypoint`)
- `npm run test:architecture:client` (fails in environment: pre-existing vuetify architecture guardrail violations)
- `npm run test:architecture:shared-ui` (pass)
- `npm run test:architecture:db` (pass)

FINAL COMMIT HASH
- `a01bb6aaa6186e69981a4db4f6258054d85cbf39`
