# Finding 008
- Severity: Low
- Pattern type: AI slop; Repository client/option plumbing duplication
- File + line(s): `packages/workspace/workspace-service-core/src/shared/repositories/workspaces.repository.js:29-47`, `packages/workspace/workspace-service-core/src/shared/repositories/invites.repository.js:92-96`, `packages/workspace/workspace-console-service-core/src/shared/repositories/settings.repository.js:52-55`
- Why it can drift/fail: The same `resolveClient`/`resolveQueryOptions` plumbing is re-implemented across repositories, often with slightly different option handling. This pattern is duplicated boilerplate that obscures actual repository logic.
- Concrete impact: Subtle differences in transaction/forUpdate handling across repos and more work to update common behavior (e.g., adding default options).
- Exact remediation: Centralize repository plumbing in a shared utility (e.g., `resolveRepoClient(dbClient, options)` and `resolveQueryOptions(options)`) and reuse across repositories.

---
Summary:
- Added shared `resolveQueryOptions` and `resolveRepoClient` helpers in `@jskit-ai/jskit-knex`.
- Replaced local resolveClient/resolveQueryOptions plumbing in workspace service workspaces/invites repositories and console settings repository.

Tests:
- `npm -ws --if-present test` (failed: missing toolchain/deps across workspace, e.g. `jskit-app-scripts`, `vitest`, multiple `ERR_MODULE_NOT_FOUND`)
- `npm run test:framework` (failed: migration tracker row mismatch and inline options error)
- `npm run test:architecture:client` (pass)
- `npm run test:architecture:shared-ui` (pass)
- `npm run test:architecture:db` (pass)

Final commit: b7ae5cb6359f9b0fbc8022452509cb94bde8bd0d
