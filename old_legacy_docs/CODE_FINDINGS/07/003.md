# Q7: Performance — unbounded concurrency resolving user summaries

## Finding
- Location: `packages/chat/chat-core/src/shared/service.js:1227`
- Behavior: `Promise.all` over `resolveUserSummary`, which hits the DB.

## Impact
Large user lists cause DB call spikes and increased latency.

## Minimal refactor
Add `userProfilesRepository.findByIds` and resolve summaries in one query, or wrap with a concurrency-limited pool.
