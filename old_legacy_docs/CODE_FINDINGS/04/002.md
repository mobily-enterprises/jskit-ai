# Q4: Convenient lies — alerts preview refresh failure discarded

## Finding
- Location: `apps/jskit-value-app/src/views/alerts/useAlertsView.js:145-150`
- Behavior: `refreshPreview` failure is discarded on mount; view continues without signaling missing preview state.

## Why risky
User never knows preview refresh failed; UI appears healthy while data is missing.

## Better error strategy
Set a lightweight banner/toast or store `error` state to indicate preview refresh failed, with retry guidance.
