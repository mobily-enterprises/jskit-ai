# Q4: Convenient lies — realtime publish failure hides error details

## Finding
- Location: `packages/runtime/action-runtime-core/src/shared/realtimePublish.js:110-114`
- Behavior: Realtime publish failure returns `false` without error details; easy to ignore.

## Why risky
Actions look successful even when realtime event never emits; root cause lost.

## Better error strategy
Log with action ID + event metadata or return a structured `{ ok:false, error }` result.
