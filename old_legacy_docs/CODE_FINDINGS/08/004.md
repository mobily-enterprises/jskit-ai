# Q8: Async correctness — alerts preview refresh races

## Finding
- Locations: `apps/jskit-value-app/src/app/state/alertsStore.js:340, 468` plus the listener triggers at `:214, 224, 238, 255, 276`.
- Behavior: Multiple triggers can overlap; refresh writes can race and apply stale data.

## Risk
Stale UI state and non-deterministic data.

## Suggested fix
Guard with a request token or cancel in-flight refreshes using `AbortController`; only apply latest response.
