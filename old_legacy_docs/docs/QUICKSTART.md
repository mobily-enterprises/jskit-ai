# Quickstart

Use the one-off `npx` flow.

```bash
mkdir my-work && cd my-work
npx @jskit-ai/create-app my-app --interactive
cd my-app
npm install
npx @jskit-ai/jskit add db --provider mysql
npm run dev
npm run server
```
