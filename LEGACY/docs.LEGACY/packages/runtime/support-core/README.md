# @jskit-ai/support-core

Shared runtime primitives for JSKIT server architecture.

## What this package does

- Defines stable container tokens used across runtime packages.
- Provides strict normalization helpers used by providers and kernels.
- Provides deterministic sorting helpers for stable outputs.

## Why this exists

To avoid duplicated utility logic and keep low-level runtime helpers in one place.
