# Finding 031
- Severity: Low
- Pattern type: AI slop; ID list normalization duplication
- File + line(s): `packages/chat/chat-core/src/shared/repositories/shared.js:54-60`, `packages/social/social-core/src/shared/repositories/shared.js:53-58`
- Why it can drift/fail: `normalizeIdList` is duplicated across chat and social repositories with identical logic. This is shared utility behavior that should be centralized rather than copied.
- Concrete impact: Divergent list-normalization semantics between domains if one copy changes; unnecessary maintenance overhead.
- Exact remediation: Move `normalizeIdList` into a shared repository utility module and import it in both domains.

---
Summary:
- Added normalizeIdList helper to `@jskit-ai/jskit-knex` repository options (with configurable parser) and exported it.
- Replaced duplicated normalizeIdList implementations in chat and social repository shared modules with the shared helper.

Tests:
- `npm -ws --if-present test` (failed: missing executables/modules across workspace)
- `npm run test:framework` (failed: migration tracker row mismatch and `jskit` inline options error)
- `npm run test:architecture:client` (pass)
- `npm run test:architecture:shared-ui` (pass)
- `npm run test:architecture:db` (pass)

Final commit: 5dee8a3a46e9054391e14e33226982f6873e53f4
