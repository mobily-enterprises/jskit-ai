# Finding 074
- Severity: Low
- Pattern type: AI slop; `moduleSignature` and `mergeDisabled` duplicated across compose paths
- File + line(s): `packages/runtime/module-framework-core/src/shared/composeClient.js:17-35`, `packages/runtime/module-framework-core/src/shared/composeServer.js:32-50`, `apps/jskit-value-app/server/framework/composeRuntime.js:181-212`
- Why it can drift/fail: Core composition helpers are copied into multiple compose implementations. Any fix to signature ordering or disabled-module merge semantics must be mirrored everywhere.
- Concrete impact: Divergent module composition behavior between client, server, and app runtime composers.
- Exact remediation: Extract these helpers into a shared `composeUtils` module within `module-framework-core` and import them in all compose implementations.

---
Summary:
- Added shared compose utilities in module-framework-core and reused them in composeClient, composeServer, and app runtime composition.
- Keeps module signature and disabled-module merge logic centralized to prevent drift.
Tests:
- npm -ws --if-present test (fails: missing jskit-app-scripts, vitest, @jskit-ai/assistant-contracts, vue, @jskit-ai/server-runtime-core, @fastify/type-provider-typebox, openai, @supabase/supabase-js, @fastify/cookie, @jskit-ai/surface-routing, @jskit-ai/cli-entrypoint; web-runtime-core guardrail error; workspace-console-core missing module)
- npm run test:framework (fails: migration tracker count mismatch; stage12 cutover expects legacy-surface but hits missing @jskit-ai/cli-entrypoint)
- npm run test:architecture:client (fails: vuetify imports in web shell template)
- npm run test:architecture:shared-ui
- npm run test:architecture:db
Commit: 1376ef1a9c0faed237ad0f441753b9fa09468923
