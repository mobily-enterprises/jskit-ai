# Finding 037
- Severity: Low
- Pattern type: AI slop; Test setup duplication
- File + line(s): `apps/jskit-value-app/tests/client/realtimeRuntime.vitest.js:208-248`, `apps/jskit-value-app/tests/client/realtimeRuntime.vitest.js:239-248`
- Why it can drift/fail: Multiple tests re-create the same `createRealtimeRuntime` setup with minor surface/permission variations. This is repetitive scaffolding that makes tests harder to maintain and update.
- Concrete impact: Inconsistent test setup behavior if one instance changes, and higher maintenance cost when runtime initialization changes.
- Exact remediation: Extract a test helper that constructs the runtime given overrides (surface, permissions, workspace slug) and reuse it across tests.

---
WONT:
- Skipping because this is client-side vitest code and you asked to avoid client-focused work until you say otherwise.
- Signal to revisit: when client-side refactors are unblocked or you want test setup helpers consolidated.

---
DONE:
- Summary: Extracted shared realtime runtime test setup helper to remove duplicated `createRealtimeRuntime` scaffolding in client realtime runtime tests.
- Tests:
  - node --check apps/jskit-value-app/tests/client/realtimeRuntime.vitest.js (pass)
  - npm -ws --if-present test (fails: missing workspace deps/tools + pre-existing failures)
  - npm run test:framework (fails: pre-existing migration tracker mismatch + missing `@jskit-ai/cli-entrypoint`)
  - npm run test:architecture:client (fails: pre-existing vuetify guardrail failures)
  - npm run test:architecture:shared-ui (pass)
  - npm run test:architecture:db (pass)
- Final commit: 85db9514f7361f14d8542e1626a2193c181a5fb4
