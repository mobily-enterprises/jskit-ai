# Finding 020
- Severity: Low
- Pattern type: AI slop; Metadata JSON helper duplication
- File + line(s): `packages/billing/entitlements-knex-mysql/src/shared/repository.js:128-136`, `packages/billing/billing-knex-mysql/src/shared/repository.js:774-782`
- Why it can drift/fail: `normalizeMetadataJsonInput` is duplicated across billing repositories with identical behavior. This is shared serialization logic that should not be copy-pasted.
- Concrete impact: Divergent metadata serialization behavior if one copy changes; unnecessary maintenance overhead.
- Exact remediation: Centralize metadata JSON normalization in a shared repository utility and reuse in both repositories.

---
Summary:
- Added `normalizeMetadataJsonInput` to `@jskit-ai/jskit-knex` repository options and exported it.
- Replaced duplicated metadata JSON normalization in billing and entitlements repositories with the shared helper.

Tests:
- `npm -ws --if-present test` (failed: missing executables/modules across workspace)
- `npm run test:framework` (failed: migration tracker row mismatch and `jskit` inline options error)
- `npm run test:architecture:client` (pass)
- `npm run test:architecture:shared-ui` (pass)
- `npm run test:architecture:db` (pass)

Final commit: ab2bdd3c9343567fa5fba8e5308be363f82bee7b
