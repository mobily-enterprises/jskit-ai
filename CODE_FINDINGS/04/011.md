# Q4: Convenient lies — socket inbound errors swallowed

## Finding
- Location: `packages/realtime/realtime-server-socketio/src/shared/registerRealtimeServerSocketio.js:1314-1326`
- Behavior: Inbound message handler failures are swallowed; only generic protocol error emitted without context.

## Why risky
Operators lack error context; clients see generic failures without actionable details.

## Better error strategy
Log the error with socket/user IDs and emit a failure code/metric for debugging.
