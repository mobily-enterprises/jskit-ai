# Q5: Hidden global state — global query client cache

## Finding
- Location: `apps/jskit-value-app/src/app/queryClient.js:16-30`
- Behavior: Module-level `QueryClient` cache.

## Why risky
App/console surfaces can share cache unintentionally; tests polluted by stale queries.

## Suggested fix
Create `createQueryClient()` factory per app mount; clear on signout and in tests.
