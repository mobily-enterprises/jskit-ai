# Q5: Hidden global state — assistant transcripts service mutates module scope

## Finding
- Location: `packages/ai-agent/assistant-transcripts-core/src/shared/service.js:26-44, 300-307`
- Behavior: Module-level `AppError`, `resolveRolePermissions`, `hasPermission` overwritten per `createService`.

## Why risky
Cross-instance bleed and test pollution.

## Suggested fix
Bind overrides inside the factory and avoid module-level mutation.
