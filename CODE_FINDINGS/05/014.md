# Q5: Hidden global state — chat service mutates module-level AppError

## Finding
- Location: `packages/chat/chat-core/src/shared/service.js:23-25, 893-897`
- Behavior: Module-level `AppError` mutated in `createService`.

## Why risky
Different chat services clobber each other; tests can become flaky.

## Suggested fix
Use a local `AppError` constant inside `createService`.
