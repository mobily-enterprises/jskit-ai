# Q5: Hidden global state — observability payload depends on globals

## Finding
- Location: `apps/jskit-value-app/src/platform/observability/browserErrorReporter.js:88-115`
- Behavior: Payload enrichment depends on `import.meta.env` and `window/navigator` globals.

## Why risky
Environment-specific behavior and SSR hazards; tests can be non-deterministic.

## Suggested fix
Inject environment info or guard behind a runtime adapter.
