# Q5: Hidden global state — test monkey patch of fetch

## Finding
- Location: `apps/jskit-value-app/tests/authServiceNoAmbientIdentityLookup.test.js:58-79`
- Behavior: Test monkey patches `globalThis.fetch`.

## Why risky
If not restored, patch bleeds across tests; global side effects hide real failures.

## Suggested fix
Ensure `restore()` in `finally`, or refactor services to accept a `fetch` dependency to avoid global patching.
