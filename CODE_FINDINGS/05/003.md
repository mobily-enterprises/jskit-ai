# Q5: Hidden global state — command tracker singleton

## Finding
- Location: `apps/jskit-value-app/src/platform/realtime/commandTracker.js:3-16`
- Behavior: Singleton `commandTracker` holds dedupe state globally.

## Why risky
Multiple surfaces/tests share command tracking; stale entries cause spooky dedupe behavior.

## Suggested fix
Create tracker per runtime instance and pass into `createRealtimeRuntime`/`createTransportRuntime`.
