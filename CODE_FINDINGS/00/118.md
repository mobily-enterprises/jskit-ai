# Finding 118
- Severity: High
- Pattern type: 8 Hidden transitive coupling
- File + line(s): `/home/merc/Development/current/jskit-ai/packages/billing/billing-plan-client-element/src/shared/BillingPlanClientElement.vue:563`
- Why it can drift/fail: `onCheckoutLinkClick()` emits `checkoutUrl` verbatim via `checkout:open` and `interaction`. Checkout URLs typically include session tokens or other sensitive identifiers.
- Concrete impact: Analytics/audit consumers can persist sensitive URL data, violating expected redaction/telemetry policies and creating governance drift.
- Exact remediation: Emit sanitized telemetry payloads (`{ hasCheckoutUrl, checkoutHost }` or `{ hasCheckoutUrl }`) and strip query/hash/path from any emitted/logged checkout URLs. Keep full URL only for navigation.
