# Finding 119
- Severity: High
- Pattern type: 5 Validation vs execution mismatch
- File + line(s): `/home/merc/Development/current/jskit-ai/packages/billing/billing-plan-client-element/src/shared/BillingPlanClientElement.vue:505`
- Why it can drift/fail: `invokeAction()` emits domain events (`plan-change:*`) before awaiting the action callback; success/failure is known only later.
- Concrete impact: Listeners can treat intent as completed mutation, leading to UI/state/telemetry divergence when callback fails.
- Exact remediation: Emit only `action:started` before callback; move `domainEventName` and corresponding `interaction` emit to success path after `await callback()`. If intent telemetry is needed, add explicit `*:requested` events.
