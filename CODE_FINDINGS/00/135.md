# Finding 135
- Severity: High
- Pattern type: 8 Hidden transitive coupling
- File + line(s): `/home/merc/Development/current/jskit-ai/packages/chat/chat-client-element/src/shared/ChatClientElement.vue:498`
- Why it can drift/fail: `meta`, `state`, `helpers`, and `actions` are captured once (`const meta = props.meta`, etc.), assuming stable object identity. If the parent replaces the runtime instance, the component continues using stale references.
- Concrete impact: Runtime swaps (workspace change, HMR, test harnesses) can lead to stale UI and actions against old instances.
- Exact remediation: Use `toRefs(props)` or computed getters instead of one-time aliasing; add a test that replaces `props.state` and verifies reactivity and action dispatch update.
