# Q7: Performance — N+1 / O(W*M) DM candidate discovery

## Finding
- Location: `packages/chat/chat-core/src/shared/service.js:1334, 1358-1374`
- Behavior: For each workspace ID, call `listActiveByWorkspaceId`, then for each candidate call `isBlockedEitherDirection`, `findByUserId`, `resolveUserSummary`.

## Impact
Explodes DB calls for users with many workspaces or large memberships.

## Minimal refactor
Add repository methods to fetch memberships for all `workspaceIds` in one query, batch load block lists, settings, and profiles with `IN (...)` queries, then filter in memory.
