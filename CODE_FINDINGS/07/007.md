# Q7: Performance — repeated JSON parse per row

## Finding
- Location: `packages/ai-agent/assistant-transcripts-core/src/shared/repositories/messages.repository.js:9-65`
- Behavior: Parses `redaction_hits_json` and `metadata_json` on every row.

## Impact
CPU hotspot for large pages; repeated parsing per request.

## Minimal refactor
Add an option to skip parsing for list endpoints, or store JSON in native DB JSON types so driver returns objects.
