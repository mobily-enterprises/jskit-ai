# Q7: Performance — sync file I/O at import time

## Finding
- Location: `packages/billing/entitlements-knex-mysql/src/shared/migrations.js:1-4`
- Behavior: Reads SQL templates synchronously at import time.

## Impact
Blocks event loop on cold start or worker spin‑up.

## Minimal refactor
Move to lazy `fs.promises.readFile` inside `createEntitlementMigrations` with a cached result.
