# Finding 019
- Severity: Low
- Pattern type: AI slop; Date normalization helper duplication
- File + line(s): `packages/billing/billing-knex-mysql/src/shared/repository.js:50-60`, `packages/billing/entitlements-knex-mysql/src/shared/repository.js:46-56`
- Why it can drift/fail: `normalizeDateInput` is duplicated across billing repositories with identical behavior. This is shared utility logic that should not be copy-pasted.
- Concrete impact: Divergent date normalization behavior if one copy changes, and extra maintenance overhead.
- Exact remediation: Move date normalization helpers into a shared billing repository utility module and reuse in both repositories.
