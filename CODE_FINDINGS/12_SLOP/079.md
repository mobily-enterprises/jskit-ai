# Finding 079
- Severity: Low
- Pattern type: AI slop; `normalizeArray` duplicated across social core and app stores
- File + line(s): `packages/social/social-core/src/shared/service.js:113-115`, `apps/jskit-value-app/src/app/state/workspaceStore.js:7-9`, `apps/jskit-value-app/src/app/state/consoleStore.js:4-6`
- Why it can drift/fail: The same array-normalization helper is copied into multiple files. If normalization semantics change, updates must be duplicated manually.
- Concrete impact: Inconsistent normalization between social core and UI stores.
- Exact remediation: Extract `normalizeArray` into a shared utility (e.g., web runtime core) and reuse it.
