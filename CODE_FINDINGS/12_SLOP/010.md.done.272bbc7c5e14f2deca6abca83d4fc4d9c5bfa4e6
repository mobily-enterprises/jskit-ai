# Finding 010
- Severity: Medium
- Pattern type: AI slop; Repository query helper duplication
- File + line(s): `packages/billing/entitlements-knex-mysql/src/shared/repository.js:185-210`, `packages/billing/billing-knex-mysql/src/shared/repository.js:798-825`
- Why it can drift/fail: `resolveQueryOptions`, `resolveClient`, and `applyForUpdate` are duplicated across billing repositories with near-identical logic. Any change to transaction/locking behavior must be applied in multiple places, making it easy for subtle differences to creep in.
- Concrete impact: Inconsistent lock/transaction semantics across billing data paths and extra maintenance cost when adjusting query options.
- Exact remediation: Extract shared repository helpers into a billing data utility module and reuse across repositories. Keep per-repo overrides explicit instead of duplicating core plumbing.

---
Summary:
- Added shared `applyForUpdate` to the `@jskit-ai/jskit-knex` repository options helpers.
- Replaced duplicated query option plumbing in billing entitlements/billing knex repositories with shared helpers.

Tests:
- `npm -ws --if-present test` (failed: missing toolchain/deps across workspace, e.g. `jskit-app-scripts`, `vitest`, multiple `ERR_MODULE_NOT_FOUND`)
- `npm run test:framework` (failed: migration tracker row mismatch and inline options error)
- `npm run test:architecture:client` (pass)
- `npm run test:architecture:shared-ui` (pass)
- `npm run test:architecture:db` (pass)

Final commit: 272bbc7c5e14f2deca6abca83d4fc4d9c5bfa4e6
