# Finding 004
- Severity: Medium
- Pattern type: AI slop; Billing worker/service scaffolding duplication
- File + line(s): `packages/billing/billing-worker-core/src/shared/outboxWorker.service.js:31-49`, `packages/billing/billing-worker-core/src/shared/remediationWorker.service.js:28-46`, `packages/billing/billing-service-core/src/shared/service.js:569-587`
- Why it can drift/fail: `recordGuardrail` and lease-fencing patterns are repeated across billing worker and service modules with near-identical logic. This makes it easy for behavior to diverge or for fixes to be applied inconsistently.
- Concrete impact: Divergent guardrail logging behavior, inconsistent error handling, and higher risk when adjusting retry or lease policies.
- Exact remediation: Extract `createGuardrailRecorder(observabilityService)` and a reusable lease-fencing helper (e.g., `withLeasedJob`) in a shared billing utility module, then replace the local copies.

---
Summary:
- Added `createGuardrailRecorder` and `withLeaseFence` to `@jskit-ai/billing-core` for centralized guardrail logging and lease-fence error handling.
- Replaced duplicated guardrail + lease fencing logic in billing outbox/remediation workers and billing service with shared helpers.
- Added `@jskit-ai/billing-core` dependency to `@jskit-ai/billing-worker-core`.

Tests:
- `npm -ws --if-present test` (failed: missing toolchain/deps across workspace, e.g. `jskit-app-scripts`, `vitest`, multiple `ERR_MODULE_NOT_FOUND`)
- `npm run test:framework` (failed: migration tracker row mismatch and inline options error)
- `npm run test:architecture:client` (pass)
- `npm run test:architecture:shared-ui` (pass)
- `npm run test:architecture:db` (pass)

Final commit: 4004008427deda5c06f54159eb30366f38b0d3ab
