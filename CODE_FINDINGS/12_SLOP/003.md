# Finding 003
- Severity: Medium
- Pattern type: AI slop; Vue client-element boilerplate duplication
- File + line(s): `packages/chat/chat-client-element/src/shared/ChatClientElement.vue:433-516`, `packages/billing/billing-plan-client-element/src/shared/BillingPlanClientElement.vue:280-329`
- Why it can drift/fail: Multiple client elements re-implement the same scaffolding (`DEFAULT_COPY`, props shape, `toRecord`, `normalizeVariantValue`, `resolvedVariant`/`resolvedFeatures`). Any change to variant rules or copy merging behavior must be replicated manually, which invites drift.
- Concrete impact: Inconsistent UI behavior across client elements (variant normalization, copy defaults) and higher friction when evolving shared UI policy.
- Exact remediation: Extract a shared composable (e.g., `useClientElementProps`) in a shared package (`@jskit-ai/web-runtime-core` or a new `client-element-core`) and reuse across elements. Keep per-component overrides limited to their unique behavior.
