# Finding 028
- Severity: Low
- Pattern type: AI slop; Test helper duplication
- File + line(s): `packages/tooling/jskit/test/shellPacks.test.js:13-28`, `packages/tooling/jskit/test/packageCommands.test.js:14-29`
- Why it can drift/fail: Multiple CLI test files re-implement the same helper utilities (`runCli`, `writeJsonFile`, `readJsonFile`) with identical behavior. This is copy-paste scaffolding that makes global test harness changes tedious.
- Concrete impact: Inconsistent test harness behavior if one copy changes, and extra maintenance overhead.
- Exact remediation: Extract common CLI test helpers into a shared test utility module (e.g., `packages/tooling/jskit/test/helpers.js`) and reuse across test files.

---
Summary:
- Added shared CLI test helpers (`runCli`, `writeJsonFile`, `readJsonFile`) in `packages/tooling/jskit/test/helpers.js`.
- Replaced duplicated helper implementations in shellPacks.test.js and packageCommands.test.js with shared imports.

Tests:
- `npm -ws --if-present test` (failed: missing executables/modules across workspace)
- `npm run test:framework` (failed: migration tracker row mismatch and `jskit` inline options error)
- `npm run test:architecture:client` (pass)
- `npm run test:architecture:shared-ui` (pass)
- `npm run test:architecture:db` (pass)

Final commit: db2b2400c4d5c61175559f51d2a6f2b6090c9f0e
