# Finding 012
- Severity: Low
- Pattern type: AI slop; JSON parsing helper duplication
- File + line(s): `packages/workspace/workspace-service-core/src/shared/repositories/settings.repository.js:4-15`, `packages/workspace/workspace-console-service-core/src/shared/repositories/settings.repository.js:6-15`, `packages/billing/billing-knex-mysql/src/shared/repository.js:14-20`, `packages/billing/entitlements-knex-mysql/src/shared/repository.js:112-125`
- Why it can drift/fail: `parseJsonValue` is re-implemented across multiple repositories with slightly different behaviors (fallbacks and object handling). This duplication makes it easy for parsing rules to diverge between modules.
- Concrete impact: Inconsistent JSON decoding between billing, entitlements, and workspace settings; subtle data shape differences that are hard to track.
- Exact remediation: Consolidate JSON parsing into a shared utility (e.g., in `@jskit-ai/jskit-knex` or a repository helper module) and reuse it across repositories, allowing module-specific defaults via options.
