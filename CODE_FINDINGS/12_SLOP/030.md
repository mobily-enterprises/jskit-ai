# Finding 030
- Severity: Low
- Pattern type: AI slop; AI toolchain helper duplication
- File + line(s): `apps/jskit-value-app/server/modules/ai/lib/tools/actionTools.js:13-27`, `apps/jskit-value-app/server/runtime/actions/contributors/assistant.contributor.js:13-79`
- Why it can drift/fail: The AI tooling layer and assistant action contributor both define identical text normalization (`normalizeText`, `normalizeLowerText`) and set-building (`toLowerSet`) helpers. This is copy-paste scaffolding that will drift as rules evolve.
- Concrete impact: Inconsistent normalization and filtering behavior between AI tool selection and assistant action execution.
- Exact remediation: Extract shared AI toolchain helpers into a single module and reuse across both action tool logic and action contributors.
