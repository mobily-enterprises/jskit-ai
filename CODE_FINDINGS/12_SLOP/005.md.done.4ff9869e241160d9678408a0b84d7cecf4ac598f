# Finding 005
- Severity: Medium
- Pattern type: AI slop; Normalization helper drift
- File + line(s): `packages/billing/billing-service-core/src/shared/checkoutOrchestrator.service.js:56-59`, `packages/billing/billing-service-core/src/shared/pricing.service.js:4-8`, `packages/billing/billing-service-core/src/shared/service.js:36-39`, `packages/billing/entitlements-core/src/shared/entities.js:71-82`, `packages/billing/entitlements-knex-mysql/src/shared/repository.js:142-154`
- Why it can drift/fail: Near-identical normalizers (e.g., `normalizeCurrency`, `normalizeAmount`) are defined in multiple modules with slightly different option names/semantics (`requireNonZero` vs `requirePositive`). This is classic copy-paste drift that silently changes validation behavior between subsystems.
- Concrete impact: Inconsistent validation and surprising edge-case behavior between billing services and entitlements storage, which can surface as subtle billing discrepancies.
- Exact remediation: Consolidate shared normalizers into a single module or rename helpers to explicitly encode semantics (e.g., `normalizeAmountAllowZero`, `normalizeAmountRequirePositive`). Replace local copies with imports.

---
Summary:
- Added shared billing normalizers (`normalizeCurrency`, `normalizeAmountAllowZero`, `normalizeAmountRequireNonZero`, `normalizeAmountRequirePositive`) in `@jskit-ai/billing-core`.
- Replaced duplicate normalizeCurrency/normalizeAmount helpers in billing service and entitlements storage with explicit shared helpers.
- Added `@jskit-ai/billing-core` dependency to entitlements core and entitlements knex mysql packages, and re-exported the named amount normalizers.

Tests:
- `npm -ws --if-present test` (failed: missing toolchain/deps across workspace, e.g. `jskit-app-scripts`, `vitest`, multiple `ERR_MODULE_NOT_FOUND`)
- `npm run test:framework` (failed: migration tracker row mismatch and inline options error)
- `npm run test:architecture:client` (pass)
- `npm run test:architecture:shared-ui` (pass)
- `npm run test:architecture:db` (pass)

Final commit: 4ff9869e241160d9678408a0b84d7cecf4ac598f
