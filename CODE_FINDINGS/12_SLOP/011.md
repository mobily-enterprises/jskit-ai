# Finding 011
- Severity: Low
- Pattern type: AI slop; JSON serialization helper duplication
- File + line(s): `packages/workspace/workspace-service-core/src/shared/repositories/settings.repository.js:41-49`, `packages/workspace/workspace-console-service-core/src/shared/repositories/settings.repository.js:41-48`
- Why it can drift/fail: Both workspace and console settings repositories implement identical `toDbJson` helpers. This is copy-paste code that invites divergence if serialization behavior needs to change (e.g., enforcing object shapes or handling invalid input).
- Concrete impact: Inconsistent JSON persistence behavior across workspace vs console settings and higher maintenance cost when fixing serialization rules.
- Exact remediation: Centralize JSON serialization in a shared repository utility and reuse across both settings repositories.
