# Finding 134
- Severity: Low
- Pattern type: AI slop; Duplicate `normalizeTableNames` helper
- File + line(s): `packages/billing/entitlements-knex-mysql/src/shared/repository.js:166-174`, `packages/billing/entitlements-knex-mysql/src/shared/migrations.js:18-26`
- Why it can drift/fail: Table name normalization is duplicated between repository and migration helpers. Any change to naming rules must be applied in both.
- Concrete impact: Mismatch between migration and repository table names if only one changes.
- Exact remediation: Extract `normalizeTableNames` into a shared entitlements-knex utility.

Summary:
- created shared `tableNames.js` utility with `DEFAULT_TABLE_NAMES` and `normalizeTableNames`
- removed duplicated table-name normalization logic from repository and migrations modules
- ensured both SQL migration rendering and repository queries resolve table names through the same helper

Tests:
- `npm -ws --if-present test` (fails: missing workspace deps/tools like `jskit-app-scripts`, `vitest`, `@jskit-ai/server-runtime-core`, `@fastify/type-provider-typebox`, `@supabase/supabase-js`)
- `npm run test:framework` (fails: migration tracker count mismatch + missing `@jskit-ai/cli-entrypoint`)
- `npm run test:architecture:client` (fails: `vuetify`/`@mdi/js` imports in web-shell-host template)
- `npm run test:architecture:shared-ui`
- `npm run test:architecture:db`

Commit: 2b2f7df3137513e517e95a38e71d52c17e2c36c7
