# Finding 046
- Severity: Low
- Pattern type: AI slop; UI string helper duplication
- File + line(s): `apps/jskit-value-app/src/views/console/useConsoleBillingPlansView.js:389-396`, `apps/jskit-value-app/src/views/console/useConsoleBillingProductsView.js:57-63`
- Why it can drift/fail: `shortenProviderPriceId` is duplicated across billing console views with identical behavior. This is shared UI helper logic that should be centralized.
- Concrete impact: Divergent display formatting if one copy changes; unnecessary maintenance overhead.
- Exact remediation: Extract `shortenProviderPriceId` into a shared billing UI helper module and reuse in both views.

---
WONT FIX
Reason:
- This finding is client-side (`apps/jskit-value-app/src/views/...`), and you asked me to avoid client-side work while templates are being rewritten.

Revisit signal:
- Once client-side work is unblocked, I can centralize `shortenProviderPriceId` in a shared billing UI helper.

---
DONE SUMMARY
- Extracted `shortenProviderPriceId` into `apps/jskit-value-app/src/views/console/providerPriceId.js`.
- Updated plans/products billing console views to import the shared formatter.
- Removed duplicated formatter implementations from both views.

TESTS RUN
- `npm -ws --if-present test` (fails in environment: missing workspace executables/dependencies and module resolution errors)
- `npm run test:framework` (fails in environment: migration tracker mismatch + missing `@jskit-ai/cli-entrypoint`)
- `npm run test:architecture:client` (fails in environment: pre-existing vuetify architecture guardrail violations)
- `npm run test:architecture:shared-ui` (pass)
- `npm run test:architecture:db` (pass)

FINAL COMMIT HASH
- `0376692f72ed6afaf045de17d98d7f421b5f47ff`
