# Finding 043
- Severity: Low
- Pattern type: AI slop; Validation error helper duplication
- File + line(s): `apps/jskit-value-app/server/modules/alerts/service.js:7-12`, `packages/users/user-profile-core/src/shared/avatar.service.js:77-83`, `apps/jskit-value-app/server/modules/auth/lib/authErrorMappers.js:104-110`, `packages/auth/auth-provider-supabase-core/src/shared/lib/authErrorMappers.js:104-110`
- Why it can drift/fail: The same `validationError` helper is copy-pasted across multiple modules with identical behavior. This is shared error-shaping logic and should be centralized.
- Concrete impact: Divergent error payloads if one copy changes; inconsistent validation responses across domains.
- Exact remediation: Extract a shared validation error helper (e.g., `createValidationError(fieldErrors)`) in a common runtime utility module and reuse across services.

---
Resolution summary:
- Added `createValidationError` to `@jskit-ai/server-runtime-core/errors`.
- Replaced local validationError helpers in alerts, avatar service, and auth error mappers with the shared helper.

Tests:
- `npm -ws --if-present test` (fails: missing `jskit-app-scripts`/`vitest`, missing packages like `@jskit-ai/assistant-contracts`, `vue`, `@fastify/type-provider-typebox`, `@supabase/supabase-js`, plus create-app assertions)
- `npm run test:framework` (fails: migration tracker count mismatch, stage12 cutover inline options)
- `npm run test:architecture:client` (fails: existing vuetify imports in `packages/tooling/jskit/packages/web-shell-host/templates/src/main.web-shell.js`)
- `npm run test:architecture:shared-ui`
- `npm run test:architecture:db`

Commit: 39093019ee7be3fca31d65f4b538f7bc756876b1
