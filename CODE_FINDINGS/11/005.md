# Finding 005
- Severity: Low
- Pattern type: Schema looseness; Contract drift risk
- File + line(s): `apps/jskit-value-app/server/modules/alerts/schema.js:12-25`
- Why it can drift/fail: `payloadJson` is typed as `Type.Any()` (nullable), which bypasses validation and allows arbitrary structure. If the payload becomes contract-critical later, this will mask incompatibilities between client/server and make backward compatibility harder to enforce.
- Concrete impact: Invalid or unexpected payload shapes can enter the system without detection, leading to downstream errors or UI assumptions that are hard to diagnose.
- Exact remediation: Replace `Type.Any()` with a structured schema (even if permissive), e.g., `Type.Record(Type.String(), Type.Unknown())` or a concrete object shape with `additionalProperties: true` plus documented expectations.
