# Q8: Async correctness — alerts store refresh preview rejections unhandled

## Finding
- Locations: `apps/jskit-value-app/src/app/state/alertsStore.js:214, 224, 238, 255, 276`
- Behavior: Cross-tab + realtime listeners call `refreshPreview` without catching rejections.

## Risk
Unhandled rejections and silent failures.

## Suggested fix
Use `void store.refreshPreview(...).catch(() => {})` or route through a shared `safeRefresh` that logs.
