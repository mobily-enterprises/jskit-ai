# Q2: Fake architecture — filesystem contribution pipeline is dead

## Finding
Filesystem-based routing/navigation composition is always empty.

- Definition: `apps/jskit-value-app/src/framework/filesystemContributions.js:7-8` with `import.meta.glob("../pages/**")` and `import.meta.glob("../surfaces/**")`.
- Callers: `apps/jskit-value-app/src/framework/composeRouter.js:57` and `apps/jskit-value-app/src/framework/composeNavigation.js:22`.
- Observation: `apps/jskit-value-app/src` has no `pages/` or `surfaces/` directories.

## Why it’s fake
The pipeline adds branching and deduplication logic but never produces any routes or navigation entries. It is a dead abstraction unless file-based routing is adopted.

## Suggested refactor
Remove filesystem contribution hooks until file-based routing exists, or add the missing `pages/` and `surfaces/` and commit to that model. As-is it is pure overhead.
