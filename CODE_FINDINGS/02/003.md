# Q2: Fake architecture — route mount registry indirection

## Finding
Route mount registry and resolver add indirection without overrides.

- Definition: `apps/jskit-value-app/src/framework/routeMountRegistry.js:1-47` and `apps/jskit-value-app/src/framework/composeRouteMounts.js:15-32`.
- Callers:
  - `composeSurfaceRouteMounts` used by `apps/jskit-value-app/src/framework/composeRouter.js:46` and `apps/jskit-value-app/src/framework/composeNavigation.js:11`.
  - `resolveRouteMountPathByKey` used in `apps/jskit-value-app/src/app/shells/app/useAppShell.js:108-114`, `apps/jskit-value-app/src/app/shells/admin/useAdminShell.js:132-139`, `apps/jskit-value-app/src/views/projects/routePaths.js:4-7`, `apps/jskit-value-app/src/views/social/useSocialFeedView.js:61-64`.
- Usage count: 24 references in `apps/jskit-value-app/src`, but only 4 runtime consumers.
- Note: mount override system is empty (`apps/jskit-value-app/config/urls.js:1-4`).

## Why it’s fake
This registry duplicates mount info already present in `apps/jskit-value-app/src/framework/moduleRegistry.base.js` (e.g., `mountKey` and `path`) and provides an override system that isn’t used.

## Suggested refactor
Remove the mount registry and use explicit paths already in navigation fragments or route fragments. If overrides are needed later, keep a small `ROUTE_MOUNTS` constant and export it directly without composition logic.
