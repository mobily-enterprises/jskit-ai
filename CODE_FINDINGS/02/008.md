# Q2: Fake architecture — provider contract re‑exports only used in tests

## Finding
Provider “contract” modules just re-export core types and are only used in tests.

- Definition: `apps/jskit-value-app/server/modules/billing/lib/providers/shared/providerAdapter.contract.js:1-6`, `webhookTranslation.contract.js:1-7`, `providerError.contract.js:1-7`.
- Callers: test imports only (e.g., `apps/jskit-value-app/tests/billingProviderAdapterLayer.test.js:6-11`).

## Why it’s fake
Extra wrapper files add surface area with no runtime use, creating needless indirection.

## Suggested refactor
Import directly from `@jskit-ai/billing-provider-core` in tests and remove these files.
