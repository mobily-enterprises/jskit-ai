# Server Common

This directory is for server-only code shared by multiple route slices in `users-routes`.

Subdirectories:
- `support/`: shared route-side helpers such as small registration helpers or route adapter utilities.

Do not put feature-specific route builders here.
