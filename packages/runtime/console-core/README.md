# @jskit-ai/console-core

Command and schedule runtime primitives for JSKIT console kernels.

## What this package does

- Registers CLI commands with deterministic IDs.
- Executes commands by ID with structured context.
- Registers schedules and lists them in deterministic order.
