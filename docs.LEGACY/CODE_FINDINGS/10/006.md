# Q10: Invariant not asserted — enabled module IDs must exist

## Finding
- Location: `packages/web/web-runtime-core/src/shared/clientComposition.js:54-62`

## Invariant assumed
All `enabledModuleIds` exist in the registry.

## Why risky
Unknown IDs are silently ignored; typo disables modules without warning.

## Suggested fix
Validate that unknown IDs are reported (throw/warn) and add a test for this behavior.
