# Finding 193
- Severity: Medium
- Pattern type: 3 Data-model bypass; 7 Contract drift risk
- File + line(s): `/home/merc/Development/current/jskit-ai/packages/operations/retention-core/src/shared/policy.js:90`
- Why it can drift/fail: `chatUnattachedUploadsRetentionHours` is read from `config.chat` while other retention settings use `config.retention.*`, so mis-shaped config silently falls back to 24h.
- Concrete impact: Retention policy can drift based on config layout, causing unexpected deletes or retention overruns.
- Exact remediation: Align to the canonical config shape and emit a warning/error when keys are missing or misplaced.
