# Finding 003
- Severity: Medium
- Pattern type: Quality gate gap; Lint coverage weakness
- File + line(s): `packages/tooling/config-eslint/base.js:1-14`, `packages/tooling/config-eslint/web.js:1-14`, `eslint.architecture.client.mjs:1-120`
- Why it can drift/fail: The base ESLint configuration only enables `@eslint/js` recommended rules and minimal language options. The web config only adds browser globals. The architectural ESLint config focuses on restricted imports and syntax, but does not enforce common code-quality rules (complexity, no-shadow, consistent returns, etc.). In a JS-only codebase, this leaves a wide quality gap.
- Concrete impact: Code-quality issues that are easy to catch statically can slip through reviews and tests, increasing defect risk and making refactors harder.
- Exact remediation: Add a focused set of quality rules (e.g., `no-shadow`, `consistent-return`, `complexity`, `max-depth`, `no-duplicate-imports`) and consider a quality-oriented plugin for JS (e.g., `eslint-plugin-sonarjs`). Keep architecture rules in place but supplement them with enforceable general-quality rules.
