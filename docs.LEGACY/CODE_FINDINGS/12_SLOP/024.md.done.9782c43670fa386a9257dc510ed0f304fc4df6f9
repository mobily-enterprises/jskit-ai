# Finding 024
- Severity: Low
- Pattern type: AI slop; Nullable string helper duplication
- File + line(s): `packages/chat/chat-core/src/shared/repositories/shared.js:62-69`, `packages/social/social-core/src/shared/repositories/shared.js:44-47`
- Why it can drift/fail: `normalizeNullableString` is duplicated in chat and social repositories with near-identical behavior. This is shared utility logic that should live in a common helper.
- Concrete impact: Divergent string normalization semantics across domains and unnecessary maintenance overhead.
- Exact remediation: Consolidate nullable string normalization into a shared repository utility and reuse across domains.

---
Summary:
- Added `normalizeNullableString` to `@jskit-ai/jskit-knex` repository options and exported it.
- Replaced duplicated nullable string helpers in chat and social repository shared modules with the shared helper.

Tests:
- `npm -ws --if-present test` (failed: missing executables/modules across workspace)
- `npm run test:framework` (failed: migration tracker row mismatch and `jskit` inline options error)
- `npm run test:architecture:client` (pass)
- `npm run test:architecture:shared-ui` (pass)
- `npm run test:architecture:db` (pass)

Final commit: 9782c43670fa386a9257dc510ed0f304fc4df6f9
