# Finding 016
- Severity: Low
- Pattern type: AI slop; Text normalization helper duplication
- File + line(s): `packages/runtime/action-runtime-core/src/shared/executionContext.js:1-7`, `packages/runtime/action-runtime-core/src/shared/policies.js:3-9`, `apps/jskit-value-app/server/runtime/actions/buildExecutionContext.js:5-11`
- Why it can drift/fail: `normalizeText`/`normalizeLowerText` are re-implemented in multiple core runtime modules and app-level wiring. This is shared logic and should not be duplicated across layers.
- Concrete impact: Divergent normalization semantics across runtime policy, execution context, and app wiring; unnecessary maintenance overhead.
- Exact remediation: Consolidate text normalization helpers into a single shared module and import them where needed.

---
Summary:
- Added shared `normalizeText`/`normalizeLowerText` helpers in action-runtime-core and exported them.
- Replaced duplicated text normalization in execution context, policies, idempotency, and buildExecutionContext with shared imports.

Tests:
- `npm -ws --if-present test` (failed: missing executables/modules across workspace)
- `npm run test:framework` (failed: migration tracker row mismatch and `jskit` inline options error)
- `npm run test:architecture:client` (pass)
- `npm run test:architecture:shared-ui` (pass)
- `npm run test:architecture:db` (pass)

Final commit: d0c994099935c669f4e89de79076e6999c72b122
