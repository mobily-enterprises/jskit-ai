# Finding 014
- Severity: Low
- Pattern type: AI slop; Repeated numeric normalization helpers
- File + line(s): `packages/runtime/runtime-env-core/src/shared/appRuntimePolicy.js:6-12`, `packages/chat/chat-client-runtime/src/shared/useChatRuntime.js:38-44`, `apps/jskit-value-app/src/app/state/alertsStore.js:26-32`
- Why it can drift/fail: The same `toPositiveInteger` helper is re-implemented across runtime policy, client runtime, and app state. This is low-level utility logic that should be shared to avoid divergence.
- Concrete impact: Slight differences in numeric validation semantics across layers and avoidable maintenance overhead.
- Exact remediation: Move `toPositiveInteger` into a shared utility module (e.g., `@jskit-ai/runtime-utils`) and import it where needed.

---
WONT:
- Skipping for now because this helper is duplicated across client runtime/app state and you asked to avoid client-side work until you say otherwise.
- Signal to revisit: once client-side work is unblocked or you want a shared runtime utility introduced for both client and server.

---
DONE:
- Summary: Introduced a shared `toPositiveInteger` helper in `@jskit-ai/runtime-env-core/integers` and replaced duplicated implementations in runtime policy, chat client runtime, and alerts store.
- Tests:
  - node --test packages/runtime/runtime-env-core/test/integers.test.js packages/runtime/runtime-env-core/test/appRuntimePolicy.test.js (pass)
  - node --test packages/chat/chat-client-runtime/test/useChatRuntime.test.js (fails: missing `vue` in local env)
  - npm -ws --if-present test (fails: missing workspace deps/tools + pre-existing failures)
  - npm run test:framework (fails: pre-existing migration tracker mismatch + missing `@jskit-ai/cli-entrypoint`)
  - npm run test:architecture:client (fails: pre-existing vuetify guardrail failures)
  - npm run test:architecture:shared-ui (pass)
  - npm run test:architecture:db (pass)
- Final commit: 5b5f644f4275184de02607971c197bfb2ab9ef9b
