# Finding 130
- Severity: Low
- Pattern type: AI slop; Duplicate `toStatusCode` helper in provider error mapping
- File + line(s): `packages/billing/billing-provider-paddle/src/shared/errorMapping.js:15-21`, `packages/billing/billing-provider-stripe/src/shared/errorMapping.js:15-21`
- Why it can drift/fail: Status-code extraction logic is copy-pasted across provider error mappers. Any change to parsing rules must be duplicated.
- Concrete impact: Inconsistent status handling between Paddle and Stripe provider errors if one changes.
- Exact remediation: Extract `toStatusCode` into a shared billing provider error utility.

Summary:
- introduced shared `toProviderStatusCode` helper in `billing-provider-core`
- updated Paddle and Stripe provider error mappers to consume the shared helper instead of local duplicates
- added parsing behavior coverage in `billing-provider-core` tests to keep status normalization deterministic

Tests:
- `npm -ws --if-present test` (fails: missing workspace deps/tools like `jskit-app-scripts`, `vitest`, `@jskit-ai/server-runtime-core`, `@fastify/type-provider-typebox`, `@supabase/supabase-js`)
- `npm run test:framework` (fails: migration tracker count mismatch + missing `@jskit-ai/cli-entrypoint`)
- `npm run test:architecture:client` (fails: `vuetify`/`@mdi/js` imports in web-shell-host template)
- `npm run test:architecture:shared-ui`
- `npm run test:architecture:db`
- `npm --workspace @jskit-ai/billing-provider-core test` (fails: missing `@jskit-ai/billing-core`)

Commit: 26ec39054a87a44fac5faa0c36e6f2ce69737cf5
