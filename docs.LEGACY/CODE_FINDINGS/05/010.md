# Q5: Hidden global state — client extensions cache

## Finding
- Location: `apps/jskit-value-app/src/app/loadExtensions.client.js:22-28`
- Behavior: `cachedClientExtensions` global cache.

## Why risky
HMR or dynamic extensions ignored; tests see stale modules.

## Suggested fix
Cache per app instance or allow explicit `reload` parameter.
