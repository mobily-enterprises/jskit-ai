# Q5: Hidden global state — transport runtime singleton

## Finding
- Location: `apps/jskit-value-app/src/platform/http/api/transport.js:15-35`
- Behavior: `transportRuntime` singleton captures `commandTracker` and client ID provider (and likely caches CSRF).

## Why risky
Stale CSRF across logins, cross-surface bleed, tests affected by previous runs.

## Suggested fix
Export a factory and create per app bootstrap; pass explicit dependencies and add a deterministic reset.
