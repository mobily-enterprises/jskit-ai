import { SETTINGS_PREFERENCES_OPTIONS } from "@jskit-ai/workspace-console-core/settingsModel";

export const themeOptions = [...SETTINGS_PREFERENCES_OPTIONS.theme];

export const localeOptions = [...SETTINGS_PREFERENCES_OPTIONS.locale];

export const dateFormatOptions = [...SETTINGS_PREFERENCES_OPTIONS.dateFormat];

export const numberFormatOptions = [...SETTINGS_PREFERENCES_OPTIONS.numberFormat];

export const currencyOptions = [...SETTINGS_PREFERENCES_OPTIONS.currency];

export const timeZoneOptions = [...SETTINGS_PREFERENCES_OPTIONS.timeZone];

export const avatarSizeOptions = [...SETTINGS_PREFERENCES_OPTIONS.avatarSize];
