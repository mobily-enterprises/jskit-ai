# Standard Prompt Template (Auto/Audit/Fix):

Use this template for any entry in `audit/auditList.md`.

```txt
Read these files fully and follow them strictly:
- /home/merc/Development/current/jskit-ai/apps/jskit-value-app/audit/auditList.md
- /home/merc/Development/current/jskit-ai/apps/jskit-value-app/audit/instructions-auditing.md
- /home/merc/Development/current/jskit-ai/apps/jskit-value-app/audit/instructions-fixing.md

MODE:
- AUTO (recommended)
# or
- AUDIT_PASS
# or
- FIXING_PASS

TARGET_AUDIT_ENTRY
## 02) API manifest and registration
Report file:
- `/home/merc/Development/current/jskit-ai/apps/jskit-value-app/audit/reports/02-api-manifest-registration.report.md`

Required scope:
- `/home/merc/Development/current/jskit-ai/apps/jskit-value-app/server/modules/api`
- `/home/merc/Development/current/jskit-ai/apps/jskit-value-app/server/fastify/registerApiRoutes.js`
- `/home/merc/Development/current/jskit-ai/apps/jskit-value-app/shared/apiPaths.js`

Required docs:
- `/home/merc/Development/current/jskit-ai/apps/jskit-value-app/docs/flows/01.endpoint-a-to-z.md`



Execution rules:
1) Resolve the exact entry and its report file path.
2) If MODE=AUTO:
   - Read the report file first, if it exists.
   - If report is missing OR `Broken things` is empty:
     - Run AUDIT_PASS behavior.
   - If `Broken things` has items:
     - Run FIXING_PASS behavior for top-priority open issues first (highest severity/confidence, default max 3 per pass).
     - In interactive mode, present the fix plan and ask for confirmation before applying code changes.
     - Do not run an audit in the same execution.
     - Stop after updating report status for this fixing pass.
   - Never switch domains.
3) If MODE=AUDIT_PASS:
   - Audit only that domain.
   - Read all required scope/docs from the entry.
   - Ignore `audit/premade-prompts/**` files while auditing.
   - Read related `tests/**` files for the same domain.
   - Create/update the report file using instructions-auditing.md.
   - Do not remove history from `Fixed things` or `Won't fix things`.
   - Run targeted runtime validation commands when possible; if not possible, state why.
   - Apply cross-domain dedupe checks against existing `audit/reports/*.report.md`.
4) If MODE=FIXING_PASS:
   - Read the report file first.
   - Ignore `audit/premade-prompts/**` unless you are explicitly asked to modify prompt assets.
   - Fix issues from `Broken things` for this domain.
   - Move resolved items to `Fixed things` and non-actioned decisions to `Won't fix things` using instructions-fixing.md.
   - Update linked issues in other report files when cross-domain duplicates exist.
5) Save the report file.
6) Return a concise summary with:
   - report path
   - issue IDs changed
   - remaining broken count by severity
   - tests reviewed/run
   - runtime validation commands run (or not run + reason)

If TARGET_AUDIT_ENTRY is ambiguous, ask one clarification question and stop.
Do not work on any other audit entry.
```
